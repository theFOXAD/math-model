# Q1 外部评审整改记录

评审基线：`csh` 分支 `0b54f93`。整改分支：`fix/csh-q1-review-remediation`。本文件记录处置结论，不把评审文本当作新的题面约束。

## 处置结论

| 编号 | 状态 | 整改与证据 |
|---|---|---|
| A1/A2 | 部分接受、边界澄清 | 当前 128/74/35 维结果明确降级为确定、可审计的 Q1 基线，不再称作附件2 BERT/COVAREP/OpenFace 同空间特征；`q1_report.md` 的 CKA+置换审计显示两者表征一致性低，支持“不能直接互换”，但不验证时间对齐或情感有效性。当前环境没有相应运行库、模型权重或 OpenFace 可执行文件，故未伪造768维语义特征。后续若替换后端，须锁定模型版本/权重哈希并发布新 run ID。 |
| A3 | 已整改 | 视觉采样率由5 fps升至10 fps；全量1917锚点中仅168个（8.8%）含不超过1个视觉采样帧，旧评审为731个（38.1%）。independent-500 是固定长度序列：不足500位填充，超过500位确定性等间隔采样；它比 aligned-50 提供更细的时间检查，但不代表无损完整动态。 |
| A4 | 已整改 | 新增 `q1_features_unaligned.npz`，音频/视觉分别为 `100×500×74`、`100×500×35`，同时保留 aligned-50。 |
| B1--B4 | 已整改 | 对齐轴改为实际解码音视频时长最大值；删除末锚点扩张和无效峰值归一；不变量验证覆盖实际解码轴、半开区间和逐模态 mask。 |
| B5/B9/B11 | 已整改 | 人脸检出率落盘并成图；对齐表加入16 kHz样点对应毫秒、10 fps数组索引、源帧号与PTS毫秒。 |
| B6/B7 | 已整改 | 保存 `observed_*`，统一空文本、分组、行数与图数口径；结果为57条数据行（54逐样本+3聚合），另有表头。 |
| B8 | 已整改 | 典型图改为6个可读文本锚点、原始音频波形及区间、源视频关键帧和三模态描述符变化；CKA图改用总体值与200次置换零基线。 |
| B10 | 已整改 | Excel空单元格保留为空转写；降级为50个等长非填充时间格，`valid_text=False`、confidence=0。 |
| C2/C5/C6/C8/C9 | 已整改 | 置信度改为最小覆盖率；输出逐维统计/零方差标记；移除个人目录硬编码和绝对Python命令；题名统一为“预测”。 |
| C1/C3/C4/C7 | 后续增强 | forced/CTC 对齐、专业声学/视觉后端与单次容器解码属于依赖或性能增强，不影响本次基线正确性；若实施须独立原子任务、对照实验和新产物哈希。 |

## 全量验收摘要

- 100/100 样本处理成功；aligned 张量形状为 `100×50×128/74/35`，independent 音视频形状为 `100×500×74/35`。
- 1917个对齐锚点连续、半开、索引有界；`observed_*` 与权威 mask 公式一致。
- 88/100 文件的 MP4 `mvhd` 声明时长比实际解码音视频时间轴长超过0.2秒；声明总时长约1120.144秒，实际解码时间轴总长约786.278秒。对齐只使用实际解码时间轴。
- 18个重叠ID产生54条逐样本×模态记录和3条总体记录；56条可估计，1条因变化不足标记 `insufficient_variation`。
- 生成10张数据图和1张流程图，每张包含 SVG、300 DPI PNG 与灰度核验图。

当前根复现清单状态为 `validated`，表示本地全链校验通过；跨用户交换状态仍未 `frozen`。只有消费者完成小批次独立加载、全部接受并由 wc 发布 I10 后，才允许将相应交换包标为 `frozen`。
