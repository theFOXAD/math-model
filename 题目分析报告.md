# E题问题一分析报告：复杂场景下多模态情感预测的特征提取与时序对齐

## 1. 任务边界与结论目标

本报告只回答问题一：对附件1的100条英语视频建立可复现的文本、语音、视觉特征提取与统一时序对齐流程。问题二的预测模型和问题三的解释模型不在本阶段实现。

最终必须给出以下可核验结论，而不是仅描述流程：

1. 100条标签记录与100个视频文件是否一一对应，是否存在重复、遗漏或孤立文件；
2. 每条样本能否解码音频和视频，三模态有效长度、特征维数和质量标志是什么；
3. 每个统一序列位置如何映射回原始文本、秒级音频区间和视频帧区间；
4. 全量覆盖率、解码成功率、对齐区间合法率及典型样本对齐可回查性（不作为时间精度验证）；
5. 18条与附件2重叠的样本在标签和表征层面是否一致；
6. 工具版本、配置、随机种子、命令、输入哈希和输出哈希是否足以复算结果。

## 2. 题目与附件清单

### 2.1 权威输入

| 输入 | 作用 | 已核验事实 |
|---|---|---|
| 题面 DOCX | 约束和提交要求 | SHA-256 为 `38cec978...b4c336` |
| 外层 `E题.zip` | 赛题原始包 | 1,881,458,858 bytes；SHA-256 为 `d8fc2773...6aa5d8` |
| 内层 `E题数据.zip` | 附件1--4 | 1,884,355,282 bytes；SHA-256 为 `f895003a...604acb` |
| 附件1视频 | 问题一原始多模态输入 | 100个 MP4，合计与标签表严格一一对应 |
| `label-100.xlsx` | 转写、强度和极性标签 | 100行、100个唯一样本；57正向、25中性、18负向 |
| 附件2 `label.xlsx` | 重叠样本与 split 参照 | 4850行；附件1中18条可精确匹配，标签零冲突 |
| 附件2 `aligned_50.pkl` | 表征一致性参照 | 仅用于18条重叠样本的审计，不替代问题一自主特征 |

完整哈希、文件数和大小见 `config/q1_dataset_inventory.yaml`。原始附件只读并位于 Git 忽略目录。

### 2.2 数据初检

- 样本键定义为 `video_id + "$_$" + clip_id`；不用行号充当 ID。
- 100条标签的连续强度范围为 $[-2,\,2.6667]$，均落在题目规定的 $[-3,3]$。
- 英文转写词数范围为5--65，中位数为18；统一长度 $T=50$ 时不能简单截断超过50词的文本。
- 18条重叠样本中11条属于附件2训练集、7条属于测试集；极性和强度标签全部一致。
- 附件1不得增删或替换样本；异常只能通过质量字段、有效掩码或降级策略表达。

## 3. 模型合同

| 项目 | 合同内容 |
|---|---|
| 任务定义 | 将100个原始 MP4 与转写转换为 aligned-50 三模态特征，同时保留音频/视觉 independent-500 固定长度采样序列，并生成位置到时间、采样点、PTS 与源帧号的可逆索引。 |
| 数据 | 附件1视频和 `label-100.xlsx` 为唯一问题一输入；附件2仅作为18条重叠样本的参照，不参与自主特征拟合。 |
| 结论类型 | 覆盖率与质量统计、全量特征清单、典型对齐示例、重叠样本表征一致性及失败案例。 |
| 模型 | 一个“文本锚点驱动的分段特征—比例时间对齐”模型族；forced/CTC 对齐未来可作为同一模型族的精度升级。 |
| 数学定义 | 统一锚点、区间边界、模态特征、显式有效/缺失/填充掩码、CKA 与 Procrustes 相似度。 |
| 实现 | Python $[3.11,3.15)$；PyAV 解码，NumPy/SciPy 音频特征，OpenCV 视觉描述，确定性哈希文本表示，CSV/NPZ 输出。依赖兼容范围见第8节，安装后冻结精确版本。 |
| 验证 | ID 一一对应、流解码、shape/dtype、掩码不变量、区间单调性、典型样本回查、18条重叠样本 CKA/Procrustes；不把可视化回查等同于人工标注的时间精度验证。 |
| 风险 | ASR/forced alignment 不可用时标记 `method=proportional,is_fallback=true`；单模态解码失败时保留样本并置对应有效掩码为 False。 |

## 4. 假设及依据

### 4.1 关键假设

1. **关键：标签表转写覆盖完整语句。** 依据是题面把 `text` 定义为对应视频片段的英文转写，且100条标签与视频严格匹配。若抽检发现严重不符，比例对齐失效，必须切换 ASR/CTC 并重新验证。
2. **关键：MP4容器中的音视频流共享同一片段时间原点。** 依据是题面将每个 MP4 定义为一个完整视频片段。若流起始时间偏移明显，必须读取时间戳而不是按零点假定。
3. **关键：统一文本锚点提供跨模态可比较视图，但不替代各模态原生节奏。** 依据是附件2 aligned 版本也以最多50个相互对应位置组织；实现同时交付音频和视觉的 independent-500 固定长度序列及时间/源帧映射。超过500位时会确定性等间隔采样，因此该序列不是无损的全部高频特征。

### 4.2 可放宽假设

1. **可放宽：词在片段中的持续时间近似与字符权重成比例。** 首版用于可复现 fallback；可用 forced/CTC 边界替换，并以18条重叠样本和人工抽检比较误差。
2. **可放宽：每秒10帧足以描述片段级表情与运动。** 若快速表情导致信息损失，可继续提高采样率并做敏感性分析。
3. **可放宽：每个锚点内采用统计聚合足以表达局部声学和视觉状态。** 可保留更高频原始描述符供下游重新池化。
4. **可放宽：确定性文本哈希仅作为无外部权重时的可复现基线。** 它没有上下文语义能力，不能称作 BERT 或附件2同空间表示；若下游需要语义表征，应使用附件2或固定版本预训练编码器，并生成新接口 run ID。

## 5. 统一时序模型

### 5.1 样本与锚点

对样本 $i$，记视频时长为 $D_i$ 秒，清洗后的词序列为 $W_i=(w_{i,1},\ldots,w_{i,L_i})$。统一最大位置数为 $T=50$。

- 若 $L_i\leq T$，每个词构成一个锚点；
- 若 $L_i>T$，将相邻词按确定性均衡分区合并为50个连续词组，禁止截断尾部；
- 转写非空时实际文本锚点数为 $S_i=\min(L_i,T)$，位置 $S_i,\ldots,T-1$ 为填充；转写为空时为保证其他模态仍可核验，时间轴降级为50个等长非填充格，`valid_text=False` 且置信度为0。

采用一基锚点索引 $t=1,\ldots,S_i$。令第 $t$ 个锚点的字符权重为 $c_{i,t}$，累计权重为 $C_{i,t}=\sum_{k=1}^{t}c_{i,k}$，则 proportional 边界定义为

$$
b_{i,t}=D_i\frac{C_{i,t}}{C_{i,S_i}},\qquad b_{i,0}=0.
$$

锚点 $t$ 对应半开区间 $[b_{i,t-1},b_{i,t})$；因此首段从0开始、末段在 $b_{i,S_i}=D_i$ 结束，不访问未定义边界。接口字段 `token_index` 使用零基值 $t-1$。音频样点和视频帧索引通过实际采样率与逐帧时间戳映射，所有索引区间均为左闭右开。

### 5.2 文本特征

每个锚点生成 $D_t=128$ 维确定性文本表示。对锚点内的字符三元组、词形和左右一阶上下文计算稳定 SHA-256，采用 signed hashing 累加到128维，并做 $L_2$ 归一化；再以固定槽位编码词长、位置、大小写、数字和标点比例。该表示不使用附件外情感数据，也不拟合标签。

设事件集合为 $G_{i,t}$，哈希桶为 $h(g)$、符号为 $s(g)\in\{-1,1\}$，则

$$
x^{(text)}_{i,t,j}=\frac{\sum_{g\in G_{i,t}}s(g)\mathbf{1}[h(g)=j]}{\max\left(\left\|\sum_{g\in G_{i,t}}s(g)e_{h(g)}\right\|_2,\varepsilon\right)}.
$$

### 5.3 语音特征

音频统一解码为单声道16 kHz。每个锚点区间内以25 ms窗、10 ms步长计算：

- 40维 log-Mel 均值；
- 13维 MFCC 均值；
- 13维一阶 MFCC 差分均值；
- 8维韵律/质量统计：短时能量均值与标准差、过零率均值与标准差、谱质心均值与标准差、有效帧比、区间时长。

合计 $D_a=74$：40维 log-Mel、13维 MFCC、13维一阶差分，加8维逐帧动态量（能量、对数能量、过零率、谱质心、谱带宽、RMS、峰值与谱平坦度）；锚点特征为区间内帧均值。区间无有效音频帧时不以零值推断缺失，而是令 `valid_audio=False`，数值张量仅作为占位。

### 5.4 视觉特征

视频按时间戳采样至目标10 fps。每帧计算并在锚点区间内聚合：

- 24维 HSV 直方图；
- 6维人脸可见与几何量（检测率、中心、宽高与面积比）；
- 3维运动量（光流幅值均值、标准差和高分位数）；
- 2维亮度与边缘密度。

合计 $D_v=35$。人脸未检出不等于视觉模态缺失：此时人脸子特征为零但 `valid_vision` 仍由视频帧是否可解码决定，并另行记录检测率。

### 5.5 掩码与填充

权威语义为：

$$
o^{(a)}=v^{(a)}\land\neg m^{(a)}\land\neg p,
\qquad
o^{(v)}=v^{(v)}\land\neg m^{(v)}\land\neg p.
$$

其中 $v$ 表示自然有效，$m$ 表示人工注入缺失，$p$ 表示填充。附件1不人工注入缺失，因此两类 `injected_missing` 均为 False，但字段仍必须存在。禁止用特征是否全零推断缺失。

## 6. 输出合同

### 6.1 问题一可复现产物

- `results/q1_feature_manifest.csv`：逐样本×模态质量与溯源汇总；
- `results/q1_samples.csv`：100行样本元数据、标签和序列长度；
- `results/q1_features.npz`：`text float32[N,50,128]`、`audio float32[N,50,74]`、`vision float32[N,50,35]` 以及全部显式掩码；
- `results/q1_features_unaligned.npz`：音频/视觉 independent-500 固定长度采样序列、有效掩码、秒级坐标及视觉源帧号；
- `results/q1_alignment.csv`：每个非填充锚点的文本—秒—音频样点—10 fps抽样帧—源帧号—PTS 映射；
- `results/q1_data_quality.csv`、`results/q1_feature_statistics.csv`：声明/实际解码时长、人脸检测率、逐维统计与零方差标记；
- `results/q1_overlap_similarity.csv`：18条重叠样本的逐模态 CKA/Procrustes；
- `results/q1_overlap_permutation.csv`：总体 CKA 的200次确定性置换零基线；
- `results/q1_normalization.json`：明确记录 `method=none`，表示问题一交付原始描述符，不声称已做训练集标准化；
- `results/q1_checksums.sha256`、`results/q1_README.md`、运行日志和复现清单。

上述文件是问题一题目产物，不作为跨用户 I01 发布，因此不写入合同外的 `split=attachment1`，也不伪造其余82条的 train/valid/test 身份。18条重叠样本的已知 split 只用于审计表。

### 6.2 与 I01/I02 的衔接

问题一结果经人工核验后，可作为后续 I01/I02 生产脚本的输入，但当前原子任务不发布或冻结跨用户接口：

- I01 必须单独基于附件2既有 train/valid/test 生产，`valid_text=True` 当且仅当该位置为非空文本锚点且不是填充；标准化只能在附件2 train 拟合，并完整写入 `normalization.json`。
- I02 只能引用已发布 I01 的 `sample_id`。每个非填充锚点记录秒级起止、音频样点索引、视频帧索引和 fallback 标志。
- 若团队决定把其余82条附件1样本加入 I01，必须先通过接口变更请求增加合法 split 值；在消费者确认前不得发布。

proportional 对齐置信度定义为可复算的区间质量分数，而非真实边界概率。对持续时间 $\Delta_{i,t}=b_{i,t}-b_{i,t-1}>0$ 的锚点，音频窗长 $w=0.025$ 秒、步长 $h=0.010$ 秒、视频目标采样率 $f_v=10$ fps。期望数量采用确定的向下/向上取整：

$$
E^{(a)}_{i,t}=\max\left(1,1+\left\lfloor\frac{\max(0,\Delta_{i,t}-w)}{h}\right\rfloor\right),
\qquad
E^{(v)}_{i,t}=\max(1,\lceil \Delta_{i,t}f_v\rceil).
$$

若实际成功解码数量为 $n^{(a)}_{i,t}$、$n^{(v)}_{i,t}$，则 $q^{(a)}_{i,t}=\min(1,n^{(a)}_{i,t}/E^{(a)}_{i,t})$、$q^{(v)}_{i,t}=\min(1,n^{(v)}_{i,t}/E^{(v)}_{i,t})$。若文本锚点为空或 $\Delta_{i,t}\leq0$，置信度直接为0；否则

$$
\gamma_{i,t}=\begin{cases}
0, & \text{文本锚点为空},\\
\min(q^{(a)}_{i,t},q^{(v)}_{i,t}), & \text{否则}.
\end{cases}
$$

该分数范围为 $[0,1]$，只表示两种已解码模态在比例区间内的最小覆盖质量；若 forced/CTC 后续接入，必须定义独立且不可混用的置信度标定。

## 7. 验证设计

### 7.1 机械不变量

1. ID唯一且100条标签、视频、输出严格同序；
2. aligned 数组首维为100、时间维为50；independent 音视频数组首维为100、时间维为500；dtype符合合同；
3. `sequence_length` 等于非填充位置数；注入缺失不落入填充区；
4. 对齐区间单调、不重叠、首端为0、末端为实际解码音视频时长的较大值；模态尾部不足由对应 mask 表达；
5. 音频样点和视频帧区间均为合法左闭右开索引；
6. 标签映射固定为 Negative=0、Neutral=1、Positive=2，强度保持原量纲。

### 7.2 质量指标

- 样本覆盖率、音频流解码率、视频流解码率；
- 每模态有效位置率、填充率、人脸检测率；
- 对齐时间覆盖率与区间边界误差；
- 特征 NaN/Inf 比率和常数维比例；
- 典型样本的文本—时间—音频—帧段人工回看。

`q1_feature_manifest.csv` 固定为逐样本×模态三行，区分 `container_declared_duration_sec`、`decoded_timeline_duration_sec` 与 `stream_duration_sec`，并记录 native/aligned 粒度、aligned/unaligned 路径、哈希、状态和警告。`q1_README.md` 给出 CSV/NPZ 读取命令、索引单位和字段解释。

### 7.3 18条重叠样本审计

对每个模态分别审计。将18条重叠样本的共同有效位置展平为 $M$ 行，自主特征记为 $X\in\mathbb{R}^{M\times d_x}$，附件2参照记为 $Y\in\mathbb{R}^{M\times d_y}$。本方法的维度依次为文本/音频/视觉 $(128,74,35)$，附件2参照维度为 $(768,74,35)$。

令 $H=I_M-\frac{1}{M}\mathbf{1}\mathbf{1}^{\top}$，$K=HXX^{\top}H$，$L=HYY^{\top}H$，计算中心化线性 CKA：

$$
\operatorname{CKA}(X,Y)=\frac{\langle K,L\rangle_F}{\|K\|_F\,\|L\|_F}.
$$

CKA 允许 $d_x\neq d_y$。Procrustes 前分别对中心化 $X,Y$ 做薄 SVD，取共同秩 $r=\min(M-1,d_x,d_y)$ 的得分矩阵 $Z_X=U_X\Sigma_X\in\mathbb{R}^{M\times r}$、$Z_Y=U_Y\Sigma_Y\in\mathbb{R}^{M\times r}$，再求

$$
R^*=\arg\min_{R^{\top}R=I_r}\|Z_XR-Z_Y\|_F,
\qquad
r_{\mathrm{Proc}}=\frac{\|Z_XR^*-Z_Y\|_F}{\|Z_Y\|_F+\varepsilon}.
$$

两项指标用于描述表征一致性，不设臆造的“通过阈值”；总体 CKA 另与200次行置换零基线比较，避免把逐样本均值误当总体关系。由于附件2没有秒级时间戳，两侧只能按相同位置编号配对，故 CKA/Procrustes 既不验证真实时间对齐精度，也不能单独判断情感识别有效性。每个模态独立报告 $M,r$ 和有效样本数。当 $M<2$、$\|K\|_F\|L\|_F=0$ 或共同秩 $r<1$ 时，不输出虚构数值，而是在结果表设置 `status=insufficient_variation`。

## 8. 实现步骤与候选图表

1. 校验输入哈希和100条 ID；
2. 解码单条视频，验证音视频时间戳；
3. 构造最多50个文本锚点和 proportional 边界；
4. 提取 aligned-50 与 independent-500 特征并写显式掩码、秒级坐标和源帧号；
5. 通过 P1 最小闭环后批量处理100条；
6. 生成 `q1_samples.csv`、`q1_features.npz`、`q1_alignment.csv`、全量质量表与典型样本表；
7. 对18条重叠样本计算 CKA/Procrustes；
8. 生成原始数据、处理过程、最终结果三类候选图和总体流程图；
9. 通过 P2 后冻结问题一可复现产物并登记 I09；I01/I02 生产与消费者握手作为后续独立任务，不在本步骤中暗含执行。

最低依赖兼容范围冻结为：Python $[3.11,3.15)$、NumPy $[2.0,3.0)$、SciPy $[1.13,2.0)$、PyAV $[14,17)$、OpenCV $[4.10,5.0)$、pandas $[2.2,3.0)$、Matplotlib $[3.9,4.0)$、PyYAML $[6,7)$。问题一使用 CSV/NPZ，不依赖 PyArrow；后续生产含 Parquet 的 I01/I02 时必须在独立兼容环境锁定 PyArrow。环境安装完成后生成精确 `config/q1_requirements.lock.txt`；若解析到范围外版本则环境门禁失败，不能静默继续。

候选图包括标签/时长分布、词数与时长关系、解码质量；典型样本多模态时间轴、各模态有效位置、特征热图；覆盖率、对齐区间统计、重叠样本 CKA/Procrustes。

## 9. 风险、回退与失败条件

| 风险 | 触发证据 | 回退动作 |
|---|---|---|
| forced/CTC 工具不可用 | 环境中无 ASR/对齐器或无法在首日时限内验证 | 使用 proportional，显式标记 fallback，不伪装为词级强制对齐 |
| 音频流缺失或损坏 | 解码无音频帧/时间戳异常 | 保留样本，`valid_audio=False`，记录质量问题 |
| 视频帧不可解码 | 解码失败或有效帧为0 | 保留样本，`valid_vision=False`，记录质量问题 |
| 超过50词 | 词数最大65 | 连续均衡分组，不截断语义尾部 |
| 人脸未检测 | 检测率为0 | 保持视觉有效，面部子特征置零并记录检测率，禁止判为模态缺失 |
| 容器声明时长大于可解码时长 | `mvhd` 声明值与实际解码音视频时间轴相差超过0.2秒（排除正常尾差） | 对齐只使用实际解码音视频时间轴，逐样本记录域偏移风险 |
| 表征一致性低 | CKA低或Procrustes残差高 | 报告为“不可直接互换”的证据；不能据此区分特征方法差异与位置配对误差，也不据此判断情感有效性 |

## 10. 可复现性与竞赛约束

- 固定随机种子为20260923；哈希文本特征本身完全确定性。
- 所有参数集中在 `config/q1_*.yaml`；所有入口为 `scripts/q1_*.py`。
- 运行环境、包版本、输入哈希、命令、耗时和输出哈希写入复现清单。
- 2026年第二十三届中国研究生数学建模竞赛官方公告规定竞赛截止为2026-09-27 12:00（北京时间），论文 MD5 同时截止；论文 PDF 上传为9月27日14:00至9月28日24:00，附件上传为9月29日8:00至9月30日24:00，附件不超过50 MB。问题一全量原始特征需采用压缩或精简表示满足限制。
- 本技能产物为辅助草稿，必须由参赛队人工核验、改写并按规则披露 AI 使用。

## 11. 文献依据

1. Bagher Zadeh, A., Liang, P. P., Poria, S., Cambria, E., & Morency, L.-P. (2018). Multimodal Language Analysis in the Wild: CMU-MOSEI Dataset and Interpretable Dynamic Fusion Graph. ACL 2018, 2236--2246. DOI: `10.18653/v1/P18-1208`.
2. Gu, Y., Yang, K., Fu, S., Chen, S., Li, X., & Marsic, I. (2018). Multimodal Affective Analysis Using Hierarchical Attention Strategy with Word-Level Alignment. ACL 2018, 2225--2235. DOI: `10.18653/v1/P18-1207`.
3. Eyben, F., Wöllmer, M., & Schuller, B. (2010). openSMILE: The Munich Versatile and Fast Open-Source Audio Feature Extractor. ACM Multimedia. DOI: `10.1145/1873951.1874246`.
4. Baltrušaitis, T., Zadeh, A., Lim, Y. C., & Morency, L.-P. (2018). OpenFace 2.0: Facial Behavior Analysis Toolkit. FG 2018, 59--66. DOI: `10.1109/FG.2018.00019`.

上述文献用于支撑 CMU-MOSEI 的异步多模态结构、词级对齐价值及声学/视觉描述符设计；实际代码采用本文明确记录的轻量可复现实现，不声称运行了尚未安装的 openSMILE 或 OpenFace。
