# E题问题一术语与符号表

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现 | 禁止混用 |
|---|---|---|---|---|---|---|
| 样本数 | number of samples | $N$ | 附件1中唯一视频片段数量，本题为100 | 条 | 数据初检 | 不称“帧数” |
| 最大序列长度 | maximum sequence length | $T$ | 三模态统一张量的最大锚点数，本题为50 | 位置 | 统一时序模型 | 不称“视频帧数” |
| 原始词数 | raw word count | $L_i$ | 样本 $i$ 的英文转写词数 | 词 | 样本与锚点 | 不与锚点数混用 |
| 有效锚点数 | valid anchor count | $S_i$ | 样本 $i$ 的非填充统一位置数，$S_i=\min(L_i,T)$ | 位置 | 样本与锚点 | 不与 `sequence_length` 赋予不同含义 |
| 视频时长 | video duration | $D_i$ | 样本 $i$ 的实际媒体持续时间 | 秒 | 样本与锚点 | 不使用帧数代替 |
| 锚点字符权重 | anchor character weight | $c_{i,t}$ | 第 $t$ 个锚点内非空字符数量的确定性权重 | 字符 | 样本与锚点 | 不称“注意力权重” |
| 累计字符权重 | cumulative character weight | $C_{i,t}$ | 从首锚点至第 $t$ 个锚点的字符权重和 | 字符 | 样本与锚点 | 不与置信度混用 |
| 时间边界 | temporal boundary | $b_{i,t}$ | 第 $t$ 个锚点的秒级边界 | 秒 | 样本与锚点 | 不与数组索引混用 |
| 文本特征 | textual feature | $\mathbf{x}^{(text)}_{i,t}$ | 第 $t$ 个锚点的128维确定性文本表示 | 无量纲 | 文本特征 | 不称“BERT特征” |
| 语音特征 | acoustic feature | $\mathbf{x}^{(audio)}_{i,t}$ | 第 $t$ 个锚点的74维原始声学描述符 | 混合量纲；问题一不跨样本标准化 | 语音特征 | 不称“原始波形” |
| 视觉特征 | visual feature | $\mathbf{x}^{(vision)}_{i,t}$ | 第 $t$ 个锚点的35维视觉描述符 | 无量纲/像素比例 | 视觉特征 | 不称“OpenFace特征” |
| 自然有效掩码 | naturally valid mask | $v^{(m)}_{i,t}$ | 模态 $m$ 在该位置存在可解码自然观测 | 布尔 | 掩码与填充 | 不与人工缺失混用 |
| 人工注入缺失 | injected missing mask | $m^{(m)}_{i,t}$ | 模态 $m$ 在该位置被实验人工遮蔽 | 布尔 | 掩码与填充 | 不称“自然缺失” |
| 填充掩码 | padding mask | $p_{i,t}$ | 该统一位置是否为长度补齐 | 布尔 | 掩码与填充 | True 明确表示填充 |
| 有效观测 | observed position | $o^{(m)}_{i,t}$ | $v^{(m)}_{i,t}\land\neg m^{(m)}_{i,t}\land\neg p_{i,t}$ | 布尔 | 掩码与填充 | 禁止由特征等于零推断 |
| 线性中心核对齐 | centered kernel alignment | $\operatorname{CKA}(X,Y)$ | 比较两套表征几何结构的一致性 | 无量纲 | 重叠样本审计 | 不解释为分类准确率 |
| 正交 Procrustes 残差 | orthogonal Procrustes residual | $r_{\mathrm{Proc}}$ | 正交映射后两套中心化表征的归一化差异 | 无量纲 | 重叠样本审计 | 越小越一致，不与相关系数混用 |
| 情感极性 | sentiment polarity | $y_i^{(c)}$ | Negative=0、Neutral=1、Positive=2 | 类别 | 输出合同 | 0类不代表中性强度值 |
| 情感强度 | sentiment intensity | $y_i^{(r)}$ | 题面给出的连续标签，范围 $[-3,3]$ | 无量纲 | 输出合同 | 不做未经记录的缩放 |
| 比例对齐 | proportional alignment | — | 按文本字符权重将片段时长分配给连续锚点 | — | 统一时序模型 | 不称“forced alignment” |
| 对齐置信度 | alignment confidence | $\gamma_{i,t}$ | proportional fallback 的区间解码质量分数，范围 $[0,0.65]$ | 无量纲 | 输出合同 | 不解释为真实边界概率 |
| 音频解码覆盖率 | audio decoding coverage | $q^{(a)}_{i,t}$ | 区间成功解码的音频分析帧占期望数的比例 | 无量纲 | 输出合同 | 不与音频有效掩码混用 |
| 视频解码覆盖率 | video decoding coverage | $q^{(v)}_{i,t}$ | 区间成功解码的目标采样帧占期望数的比例 | 无量纲 | 输出合同 | 不与人脸检测率混用 |
| 期望音频帧数 | expected audio frame count | $E^{(a)}_{i,t}$ | 按25 ms窗、10 ms步长和显式向下取整计算，最小为1 | 帧 | 输出合同 | 不用实际解码数代替 |
| 期望视频帧数 | expected video frame count | $E^{(v)}_{i,t}$ | 按区间时长乘5 fps并向上取整，最小为1 | 帧 | 输出合同 | 不与容器原始帧率混用 |
| 降级对齐 | fallback alignment | — | forced/CTC 不可用时采用的可复现替代路线 | — | 风险与回退 | 必须设置 `is_fallback=true` |
| 左闭右开区间 | half-open interval | $[a,b)$ | 包含左端、不包含右端的索引或时间区间 | 秒或索引 | 统一时序模型 | 禁止和闭区间混用 |
